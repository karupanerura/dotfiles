[include]
    path = .gitconfig.local
[core]
    editor = emacsclient -nw
    pager  = lv
    whitespace=fix,-indent-with-non-tab,trailing-space,cr-at-eol
    excludesfile = .gitignore
[color]
    ui = always
    branch = auto
    diff = auto
    status = auto
[color "branch"]
    current = yellow reverse
    local = yellow
    remote = green
[color "diff"]
    meta = yellow bold
    frag = magenta bold
    old = red bold
    new = green bold
[color "status"]
    added = yellow
    changed = green
    untracked = cyan
[branch]
    autosetupmerge = false
    autosetuprebase = always
[rebase]
    stat = true
[merge]
    stat = true
[alias]
    st   = status
    ci   = commit
    br   = branch
    co   = checkout
    df   = diff
    lg   = log -p
    url  = config --get remote.origin.url
    tree = log --graph --pretty='format:%C(yellow)%h%Creset %s %Cgreen(%an)%Creset %Cred%d%Creset'
    wt   = whatchanged --stat
    edit-unmerged = "!f() { git ls-files --unmerged | cut -f2 | sort -u ; }; emacs `f`"
    add-unmerged  = "!f() { git ls-files --unmerged | cut -f2 | sort -u ; }; git add `f`"
    ck   = "!git status --short | awk '{print $2}' | egrep '\\.(t|p([lm]|sgi))$' | xargs -n 1 -P0 perl -wc"
[push]
    default = current
[pull]
    rebase = true
[rerere]
    enabled = true
[grep]
    lineNumber = true
